/** Route group auth — URL tetap /sign-in, /sign-up, /auth/complete (tanpa segmen grup). */
export default function AuthenticationLayout({ children }: { children: React.ReactNode }) {
  return children;
}
