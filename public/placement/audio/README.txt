# Choukai audio (placement)

Drop the master listening file here as:

`JepangKu Placement Test.mp3`

URL used by the app: `/placement/audio/JepangKu%20Placement%20Test.mp3`

Do not commit large MP3 binaries if the team prefers LFS / external hosting — keep this folder for local/dev assets.
