/**
 * Dark mode is paused until the design pass is complete.
 * Set `THEME_SWITCHING_ENABLED` to `true` to restore the theme toggle UI.
 */
export const THEME_SWITCHING_ENABLED = false;

export const FIXED_THEME = 'light' as const;
